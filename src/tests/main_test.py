import atomic_concepts.expression as expression
import atomic_concepts.function as function
import atomic_concepts.atom as atom

test = expression.Expression(function.Function("test", []))
test = atom.Atom(True)